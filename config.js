var config = {
    myself : [
    ],
    host : "http://localhost:7903",
    // data reload interval in seconds
    reload_interval : 30,
    // chart reload interval in seconds
    reload_chart_interval : 600,
    // HTML to load at top of page
    header_content_url : "http://localhost:7903/custom_content.html",
    // Default Theme
    theme : 'cyborg'
  }
